apply plugin: 'maven-publish'

group = 'com.github.ipcjs.baidu-map-sdk'
version = 'v3.0.3-SNAPSHOT'

afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                from components.release
            }
        }
    }
}
